<h1>Edit Category</h1>

<%= form_with model: @category, url: admin_category_path(@category), method: :patch, local: true do |form| %>
  <div class="form-group">
    <%= form.label :category_name, 'Category Name' %>
    <%= form.text_field :category_name, class: 'form-control', required: true %>
  </div>

  <%= form.submit 'Update Category', class: 'btn btn-primary mt-3' %>
<% end %>
<%= link_to 'Back', admin_categories_path, class: 'btn btn-secondary mt-3' %>